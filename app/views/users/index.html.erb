<h1>Users</h1>

<!--<ul>-->
  <%# @users.each do |user| %>
    <%#= image_tag user.avatar.url(:thumb)%>
<!--    <li><%#= link_to user.full_name, user %></li>-->
  <%# end %>
<!--</ul>-->



<div class="row row-cols-1 row-cols-md-5">
  <% @users.each do |user| %>
    <div class="col mb-4">
      <div class="card border-dark h-100">
        <%= image_tag user.avatar.url(:thumb), class: "card-img-top"%>
        <div class="card-body">
          <h4 class="card-title"><%= link_to user.nickname, user %></h4>
          <p class="card-text"><%= user.full_name %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>