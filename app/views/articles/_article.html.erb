<ul>
  <% articles.each do |article| %>
    <li>
      <%= link_to article.title, article %><br>
      <%= image_tag article.news_main_image.url(:thumb) %><br>
      <%= raw article.tags.map(&:name).map{ |t| link_to t, tag_path(t) }.join(' ') %>
    </li>
  <% end %>
</ul>
